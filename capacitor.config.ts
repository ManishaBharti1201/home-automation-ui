import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.homeautomation.app',
  appName: 'HomeAutomationUI',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;